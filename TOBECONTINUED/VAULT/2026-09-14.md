# 2026-09-14

## Hari ini

- FOKUS ZCODE Desktop Fase 1 (branch `arena/zcode-desktop`, repo zabaniyah221-stack/ZCODE).
- Workbench v0.0.1: toolbar Run/F5, tab per file, breadcrumb path, panel output auto-show, status bar (interpreter+run#+log), Settings font UI persist file, About — semua CI hijau.
- Aturan baru dari user: tiap shortcut WAJIB punya tombol (jalan utama); tombol tanpa fungsi dilarang mati sunyi (ngaku "belum Fase 1").
- Uji GUI lokal (Celeron N2840, RAM 3,7GB): KCEF/Chromium 150 init OK; laporan user via ~/Gambar/LAPORAN DAN PERBANDINGAN (banding VS Code).
- Temuan + fix: bridge-check terlalu dini → retry 20x; dialog "Untitled" putih → judul+ukuran eksplisit (lalu dialog DIHAPUS sementara saat UI minimal); Run kosong sunyi → guard; overlay indikator curi fokus → pindah status bar + autofocus CM6.
- Akar klik-tak-fokus: fokus Compose tak sampai ke Chromium → `KCEFBrowser.setFocus(true)` via `WebView(onCreated)` (bedah source lib 2.0.1) → KLIK-TULIS BERHASIL ✓.
- Crash `createContext(...) must not be null` (race init di defaultWebViewFactory) → factory custom tanpa requestContext → sembuh.
- UI minimal Fase fokus: HANYA editor + output + Run (+tab/breadcrumb/status). Sidebar, Buka/Simpan, dialog, Ctrl+S/O dimatikan sementara (fungsi dipertahankan).
- Run lambat → timer per tahap di status bar (`ambil Xms · py Yms · total Zms`) + log `[RUN-TIME]`.
- Neko Player: mpv mati diam-diam + file 103-byte (pesan error download) dikarantina ke ~/Musik/.rusak; watchdog restart + seed anti-duplikat; playlist 8 lagu (6 baru kedetect ✓).
- Neko CPU 85% → biang `timeout_add(350ms)` + callback return True = loop abadi tiap klik; fix sekali-tembak `update_track_once` (return False) → widget 5%, mpv 11% ✓.
- Telegram @Maelinea_bot ONLINE dua arah (tujuan: notif CI otomatis — BELUM dipasang).

## Backlog

- Notif CI otomatis via Telegram.
- Kembalikan sidebar/Buka/Simpan/dialog setelah logika stabil.
- Splash gelap (ganti layer putih start) — nanti.
- Status bar biru NOTEZ.

Terkait: [[Proyek/ZCODE]], [[HERMES x ZAQI]]
catatSore Workbench installed v4 plus riset blink plus drawer overlay. Installed v4 verified start 40 sampai 50 detik, title ZCODE, editor OK. Force-close lalu buka lagi 50 detik normal. E2E requests ketik import requests plus F5 drawer kanan 2.31.0 exit 0 total 1720ms ambil 632ms plus py 1088ms. Cek file putih index bersih Main bersih bundle cuma putih transparan, blink BUKAN dari file kita. Riset forum CEF issue 1984 white flash OSR bug upstream resmi, VS Code menutupinya pakai splash, strategi kita benar. Splash tahan plus 800ms pasca-bridge. Drawer sibling jadi OVERLAY kanan tanpa resize CEF. Veil dihapus ganggu kursor. CI merah 2x LaunchedEffect sebelum deklarasi, Modifier.align konflik overload. Kesepakatan tiap deb baru pkill plus hapus installed lama.Malam. Drawer overlay v6 verified area konten bawah toolbar isi 42 exit 0 total 1092ms. Insiden dua ZCODE crash rebutan CEF lock karena java anak yatim versi lama masih hidup saat v6 start, pelajaran kill java anak bukan cuma script launcher. Start tunggal v6b bridge 24 detik.VERDICT T1 BENAR via splash merah nempel. Merah nutup toolbar plus status tapi editor tembus hitam CEF native heavyweight selalu di atas overlay Compose. Saran teman splash di atas editor GUGUR untuk area editor. Keputusan final drawer SIBLING bukan overlay, splash untuk fase pra-CEF plus non-editor. Commit b44820a revert splash hitam.FINAL 14 Sep malam commit b44820a installed verified. Sibling drawer penuh final 12 sampai 29 autoscroll bawah scrollbar OK status ringkas exit 0 total 1037ms splash hitam normal. T1 proven overlay Compose tak bisa atas CEF native. Sibling adalah bentuk final drawer. Sisa blink resize 150ms diterima.