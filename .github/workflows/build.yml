name: Build ZCODE APK — Fase 1 & 2

on:
  push:
    branches: [ main, "arena/**" ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run strict tests (70 + 55)
        run: |
          pip install --break-system-packages pillow pytest
          python -m pytest test_zcode_fase0.py test_zcode_fase1.py -v
          bash tools/check.sh

  build:
    runs-on: ubuntu-22.04
    needs: check
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle 8.5 (pin — 9.6 incompatible with AGP 8.2.2)
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.5

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties', 'settings.gradle.kts', 'app/build.gradle.kts') }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew || echo "no gradlew"

      - name: Build Debug APK (Fase 1 & 2 — Chaquopy 3.11 embed)
        run: |
          set -o pipefail
          gradle assembleDebug --stacktrace --warning-mode all 2>&1 | tee build.log
          echo "--- tail ---"
          tail -n 200 build.log

      - name: Verify APK contains Ace + taskAffinity
        run: |
          ls -R app/build 2>&1 | head -n 50 || echo "no build dir"
          APK=$(find app/build -name "*.apk" 2>/dev/null | head -n 1)
          echo "APK: $APK"
          if [ -z "$APK" ]; then echo "❌ No APK found — build failed"; ls -lh app/build/outputs/apk/ 2>&1 | head -n 30; exit 1; fi
          ls -lh "$APK"
          unzip -l "$APK" | grep -i -E "ace.js|AndroidManifest" | head -n 20
          # Verify Chaquopy Python runtime embedded in APK
          unzip -l "$APK" | grep -i -E "libpython|libchaquopy" | head -n 10 || echo "⚠️ libpython not found — check Chaquopy packaging"
          # Check manifest has taskAffinity (via aapt dump)
          $ANDROID_HOME/build-tools/34.0.0/aapt dump xmltree "$APK" AndroidManifest.xml | grep -i "taskAffinity" || echo "check taskAffinity in manifest"

      - name: Upload APK (Fase 0 kerangka)
        uses: actions/upload-artifact@v4
        with:
          name: ZCODE-Fase2-APK
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: CI Summary
        if: always()
        run: |
          echo "### ZCODE Fase 1 & 2 Build — $(date)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Topbar: faded grey #3A4452 ≡ + (no hamburger text)" >> $GITHUB_STEP_SUMMARY
          echo "- Chaquopy 3.11 embed: Run waifu.py & pip di HP ARMv7 (arm64+armv7)" >> $GITHUB_STEP_SUMMARY
          echo "- Editor: OLED #050806 Ace 1.44.0 file:// + gutter 40px" >> $GITHUB_STEP_SUMMARY
          echo "- QuickTools + FAB above handle, Drawer text-only, Contribute -> Issues" >> $GITHUB_STEP_SUMMARY
          echo "- PTY: ketik langsung di terminal (no stdin field), Chaquopy 3.11 arm64+armv7" >> $GITHUB_STEP_SUMMARY
          ls -lh app/build/outputs/apk/debug/ >> $GITHUB_STEP_SUMMARY || true
